# Copy to frankenphp.env (ignored) to define site-level variables available to Caddy.
# These values are injected via docker-compose's env_file directive.
FRANKENPHP_DEFAULT_APP=laravel-app
FRANKENPHP_DEFAULT_HOST=laravel-app.localhost:80
PROJECT_APP=project
PROJECT_HOST=project.localhost:80

# Example extra project (uncomment + adjust as needed)
# TODO_APP=todo-app
# TODO_HOST=todo.localhost:80
