{
    auto_https off
}

# Main FrankenPHP entry point. Each project/site lives in config/frankenphp/sites/*.caddy
# so they can be managed independently (and selectively ignored by git).
import ./sites/*.caddy
